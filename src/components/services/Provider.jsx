const apiUrl = "https://api.kushalshah.dev/"
const baseUrl = `${apiUrl}api/v1`
const projects = "/projects"
const images = "/images/"
const testimonial = "/testimonial"
const contact = "/contact"

export { baseUrl, projects, images, testimonial, contact }